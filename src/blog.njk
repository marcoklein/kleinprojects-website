---
layout: 'page.njk'
navParent: blog
title: Blog
---

{%- for post in collections.posts -%}
  <h2 class="mb-3">
    <a href="/{{ post.data.title | slugify }}/" class='has-text-weight-bold'>
      {{ post.data.title }}</a>
    <br/>
    <span class='is-italic'>{{ post.data.tags.join(', ') }}</span>
    &middot; {{ post.data.date | formatDate }}
    <p class='has-text-weight-light'>
      {% excerpt post %}
    </p>
  </h2>
{%- endfor -%}