---
layout: page.njk
navParent: home
title: Kleinprojects
subtitle: coding projects and blog
---

{%- for post in collections.posts -%}
  <article>
    <h3><a href="{{ post.data.title | slugify }}"><strong>{{ post.data.title }}</strong></a></h3>
    <p><small>{{ post.data.tags.join(', ') }} &middot; {{ post.data.date | formatDate }}</small></p>
  </article>
{%- endfor -%}
